#include <iostream>
#include <iomanip>

using namespace std;

int main() {
	
	const int N = 100; 

	int Arr_int1[N];
	int Arr_int2[N];

	double Arr_d1[N];
	double Arr_d1[N];

	float Arr_f1[N];
	float Arr_f2[N];

	char Arr_ch1[N];
	char Arr_ch2[N];

	int size;
	cout << "Enter massive size: ";
	cin >> size;

	int type;
	cout << "Choose filling type (1 - ascending, 2 - descending, 3 - randomly): ";
	cin >> type;

	switch (type) {
	case 1: {
		for (int i = 0; i < size; i++)
			
	}
	}

	return (0);
}